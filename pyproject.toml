[project]
name = "tradebotbuilder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "smartmoneyconcepts",
    "pandas",
    "numpy",
    "dukascopy-python",
    "python-dotenv",
    "MetaTrader5; sys_platform == 'win32'",
]

[tool.vercel]
entrypoint = "api.analizar:handler"
