[project]
name = "tradebotbuilder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "smartmoneyconcepts",
    "pandas",
    "numpy",
    "dukascopy-python",
]

[tool.vercel]
entrypoint = "api.analizar:handler"
